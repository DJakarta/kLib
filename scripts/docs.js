function codeIndenting() {	$("code.block").each(function () {		newText=$(this).text();		while (newText.lastChar()=="\n" || newText.lastChar()=="\t") {			newText=newText.slice(0,newText.length-1);		}		min=100;		temp=0;		for (i=0; i<newText.length; i++) {			temp=0;			while (newText.charAt(i)=="\t") {				temp++;				i++;			}			if (temp && temp<min) {				min=temp;			}		}		if (min==100) {			min==0;		}		tabs=new RegExp("\t{"+min+"}", "g");		newText=newText.replace(tabs, "");		newText=newText.substr((kLib.UA.short==='IE' && kLib.UA.version<9) ? 0 : 1);		$(this)[0].innerHTML='<pre></pre>';		$($(this)[0].firstChild).text(newText);	});}codeIndenting();$("code.block").click(function () {	eval($(this).text());});$k(".NYI").forEach(function () {	this.title='Not Yet Implemented';});$k(".OPI").forEach(function () {	this.title='Only Partially Implemented';});