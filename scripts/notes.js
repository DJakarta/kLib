function codeIndenting() {	$("code.block").html(function () {		newText="<pre>"+$(this).text();		while (newText.lastChar()=="\n" || newText.lastChar()=="\t") {			newText=newText.slice(0,newText.length-1);		}		newText+="</pre>";		min=100;		temp=0;		for (i=0; i<newText.length; i++) {			temp=0;			while (newText.charAt(i)=="\t") {				temp++;				i++;			}			if (temp && temp<min) {				min=temp;			}		}		if (min==100) {			min==0;		}		tabs=new RegExp("\t{"+min+"}", "g");		newText=newText.replace(tabs, "");		return newText;	});}codeIndenting();$("code.block").click(function () {	eval($(this).text());});